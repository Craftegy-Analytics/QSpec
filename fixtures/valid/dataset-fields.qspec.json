{
  "apiVersion": "qspec.dev/v1",
  "kind": "Dataset",
  "metadata": { "name": "dataset-fields" },
  "spec": {
    "dataset": {
      "fields": {
        "id": { "type": "string", "nullable": false },
        "amount": {
          "type": "number",
          "nullable": true,
          "label": "Amount",
          "semanticType": "currency",
          "format": { "currency": "USD" }
        },
        "createdAt": { "type": "datetime", "nullable": false },
        "isActive": { "type": "boolean" },
        "tags": { "type": "array" },
        "metadata": { "type": "object" }
      }
    }
  }
}
